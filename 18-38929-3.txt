1.select hiredate , job from emp where hiredate in(select min(hiredate) from emp group by deptno) order by hiredate desc

2.SELECT TO_CHAR(SYSDATE,'DAY HH24:MI:SS')AS "TIME NOW" FROM DUAL

3.

4.

5.SELECT ENAME,JOB FROM EMP WHERE JOB IN('CLERK','MANAGER','ANALYST')

6.select ename from emp where ename like '%M%'

7.select ename from emp where ename like '%LL%'OR ename like '%TT%'

8.SELECT TRIM(TRAILING '0' FROM '00abc000') FROM DUAL

9.CREATE TABLE TEACHER
(
t_id	Number(10)
t_name	Varchar2(20)
phone	Number(10)
salary	Number(7)
email	Varchar2(30)
);

Subquery

1.SELECT ENAME FROM EMP WHERE HIREDATE>(SELECT HIREDATE FROM EMP WHERE ENAME='KING')

2.SELECT ENAME FROM EMP WHERE SAL> ALL(SELECT SAL FROM EMP WHERE JOB='CLERK')

3.SELECT ENAME FROM EMP WHERE SAL>ANY (SELECT SAL FROM EMP WHERE JOB='CLERK')

4.SELECT ENAME FROM EMP WHERE SAL IN(SELECT MAX(SAL) FROM EMP GROUP BY DEPTNO)

5.SELECT ENAME FROM EMP WHERE SAL>(SELECT SAL FROM EMP WHERE ENAME='SMITH')

Joining

1.SELECT ENAME FROM EMP E,DEPT D WHERE E.DEPTNO=D.DEPTNO AND LOC='DALLAS'

2.SELECT E.ENAME FROM EMP E,EMP M WHERE E.MGR=M.EMPNO AND E.HIREDATE<M.HIREDATE

3.

4.SELECT s.grade,e.* FROM salgrade s ,emp e where e.sal between s.losal and s.hisal

5.SELECT ENAME,e.DEPTNO,DNAME FROM EMP E,DEPT D WHERE E.DEPTNO=D.DEPTNO 

Schema Diagram

1.CREATE TABLE SUBJECTS
(
SUBID NUMBER(5),
SUNME VARCHAR(20),
PRIMARY KEY(SUBID)
);

INSERT INTO SUBJECTS(SUBID,SUNME)
VALUES(1,'A')
INSERT INTO SUBJECTS(SUBID,SUNME)
VALUES(2,'A')
INSERT INTO SUBJECTS(SUBID,SUNME)
VALUES(3,'A')
INSERT INTO SUBJECTS(SUBID,SUNME)
VALUES(4,'A')
INSERT INTO SUBJECTS(SUBID,SUNME)
VALUES(5,'A')

2.CREATE TABLE PUBLISHERS
(
PUBID NUMBER(5),
PUNME VARCHAR(20),
PRIMARY KEY(PUBID),
EMAIL VARCHAR(20),
PHONE NUMBER(11)
);

INSERT INTO PUBLISHERS(PUBID,PUNME,EMAIL,PHONE)
VALUES(1,'B','','')
INSERT INTO PUBLISHERS(PUBID,PUNME,EMAIL,PHONE)
VALUES(2,'B','','')
INSERT INTO PUBLISHERS(PUBID,PUNME,EMAIL,PHONE)
VALUES(3,'B','','')
INSERT INTO PUBLISHERS(PUBID,PUNME,EMAIL,PHONE)
VALUES(4,'B','','')
INSERT INTO PUBLISHERS(PUBID,PUNME,EMAIL,PHONE)
VALUES(5,'B','','')

3.CREATE TABLE TITLES
(
TITELID NUMBER(5),
TITEL VARCHAR(20), 
PUBID NUMBER(5),
SUBID NUMBER(5),
PUBDATE DATE,
COVER VARCHAR(20),
PRICE NUMBER(8),
FOREIGN KEY (SUBID) REFERENCES SUBJECTS(SUBID),
FOREIGN KEY (PUBID) REFERENCES PUBLISHERS(PUBID),
PRIMARY KEY(TITELID)
);

4.CREATE TABLE AUTHORS
(
AUID NUMBER(5),
AUNME VARCHAR(20),
PRIMARY KEY(AUID),
EMAIL VARCHAR(20),
PHONE NUMBER(11)
);

5.CREATE TABLE TITELAUTHORS
(
AUID NUMBER(5),
FOREIGN KEY(AUID) REFERENCES AUTHORS(AUID),
TITELID NUMBER(5),
FOREIGN KEY(TITELID) REFERENCES TITELS(TITELID),
IMPORTENCE VARCHAR(20)
);